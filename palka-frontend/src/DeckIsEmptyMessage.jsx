function DeckIsEmptyMessage() {
    return (
        <div>
            <h1>Your deck is empty!</h1>
            <p>Click on the Cards to add them!</p>
        </div>
    );
}

export default DeckIsEmptyMessage;